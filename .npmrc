# 原生模块按 Electron ABI 安装（better-sqlite3 prebuild 下载，无需本机编译）。
# 升级 Electron 时同步更新 target。
runtime=electron
target=43.4.0
# better-sqlite3 12.12.0 未发布到 npm registry（GitHub 专供），仅允许根依赖的远程 tarball。
allow-remote=root
