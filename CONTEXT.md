# jobhunt-ai-helper（求职助手）

一个本地桌面求职辅助应用：追踪校招职位、按 JD 优化简历、驱动技术学习、语音模拟面试并复盘。核心原则：招聘看的是岗位匹配度。

## Language

**职位卡**:
一条校招岗位记录：公司、岗位名、JD、投递渠道、网申窗口、企业性质等（`positions` 表一行）。
_Avoid_: 岗位条目、招聘信息

**JD**:
Job Description——职位描述全文；匹配度计算与学习清单提取的输入源。

**基准简历**:
求职者的原始简历（可多份，如技术向/国企向各一份），优化简历的派生源头（`meta.baseResumeId = null`）。
_Avoid_: 原始简历、主简历

**优化简历**:
针对某个职位卡的 JD、从基准简历派生的简历版本（`meta.baseResumeId` + `meta.targetJobId` 标记）。
_Avoid_: 定制简历、精修简历

**匹配度**:
JD 与简历的契合程度：规则层 5 维度加权分（0-100）+ LLM 层定性分析。
_Avoid_: 契合度、匹配分数

**投递渠道**:
投递简历的入口（官网/牛客/猎聘/邮箱/内推等）；既是职位卡的信息字段，也是投递记录（applications）的动作字段。

**投递状态**:
职位卡在投递流程中的当前阶段，取 `applications` 最新一条记录的状态：`planned`（待投递/收藏）→ `applied`（已投）→ `interviewing`（面试中）→ `offer`/`rejected`；`withdrawn` 任意状态可入。UI 上"待投递"与"收藏"是同一状态的不同说法。
_Avoid_: 应聘状态、求职状态、投递进度

**秋招时间线**:
秋招各批次（提前批/正式批/补录）的网申窗口，由职位卡的 `start_date` / `end_date` / `batch` 表达。

**学习条目**:
一个待学/在学/已掌握的技术点（`topics` 表一行）；来源可为 JD、简历缺口、项目 techStack 或面试复盘。
_Avoid_: 学习任务、知识点

**面试复盘**:
一次模拟面试后生成的结构化评估：维度评分、薄弱点、参考答案、下一步建议（`interviews.review`）。
_Avoid_: 面试总结、面试评价
