# DESIGN.md — 求职助手（jobhunt-ai-helper）UI 设计输入 v1

> 本文档是 UI 原型的沉淀（来源：`prototypes/app-ui/app-ui.html`，分支 `prototype/app-ui`），
> 作为高保真细化（OpenDesign）的设计输入。**它不是最终设计**——三个变体方向待选定，组件与
> 视觉规范待细化。

---

## 1. 产品定位

**一句话**：本地桌面求职助手，围绕「岗位匹配度」形成闭环——**职位 → 优化简历 → 学习补缺 → 模拟面试 → 复盘改进**。

- 形态：Electron 桌面应用（Windows、本机单用户、中文）
- 核心概念（术语见 CONTEXT.md）：职位卡、基准简历、优化简历、匹配度、学习条目、面试复盘
- 技术约束（影响 UI 的）：
  - 主进程服务经 IPC 调用；agent 输出**流式渲染（打字机）**
  - 语音输入：**PTT 按住说话**（本地 ASR）
  - LLM / ASR 不可用时**分层降级**（手动功能照常，agent 功能提示配置）

## 2. 设计原则

1. **匹配度可见**——每个职位都能一眼看到「我匹配多少、缺什么」（分数 + 维度 + 依据）
2. **闭环引导**——操作按「优化 → 学习 → 面试 → 投递」的自然顺序组织
3. **信息优先**——工具型桌面应用：密度适中、操作直达，不做营销化装饰
4. **降级友好**——未配置模型/断网/ASR 缺模型时，功能降级而非整体不可用，提示清晰
5. **本地感**——桌面应用而非网页：无登录、无多用户、无云端同步暗示

## 3. 信息架构（5 个模块）

| 模块 | 核心对象 | 主要操作 |
|---|---|---|
| 职位 | 职位卡（公司/岗位/JD/渠道/网申窗口/批次/企业性质/状态） | 录入、采集（预览确认）、筛选、详情、投递状态流转（收藏→已投→面试中→offer/拒绝） |
| 简历 | 基准简历（可多份）、优化简历（派生，关联职位卡） | 创建/上传解析、编辑、A4 预览/导出 PDF、按 JD 优化（对比确认） |
| 学习 | 学习条目（优先级 1-5、三态：待学/学习中/已掌握） | 清单自动生成、人工编辑、启动 teach 聊天教学、标记状态 |
| 面试 | 面试会话、复盘记录 | 开始面试（选职位/风格）、语音问答、结束复盘、回看历史 |
| 设置 | 模型认证（提供商/API key/模型） | 配置、状态查看；未配置时各模块降级提示 |

## 4. 导航与整体布局（3 个候选变体，**待用户选定**）

### 变体 A — 专业工作台（侧边栏 + 列表/详情三栏）

```
┌────────┬──────────────────────────────────────────────┐
│ 侧边栏  │ 顶栏（当前秋招季 · 统计 · 模型状态）            │
│ logo    ├──────────┬───────────────────────────────────┤
│ 职位 ●  │ 列表列    │ 详情列                            │
│ 简历    │ (筛选+    │ (匹配度概览 · 操作按钮 · JD ·      │
│ 学习 ●  │  列表)    │  投递记录)                        │
│ 面试    │          │                                   │
│ 设置    │          │                                   │
└────────┴──────────┴───────────────────────────────────┘
```

- 导航：左侧 168px 深色栏，图标+文字，学习模块带红点提醒
- 内容：三栏（筛选列表 | 详情/活动区）；简历/学习/面试用双栏（对象列表 | 工作区）
- 顶栏：当前秋招季、统计（职位数/待投/最近截止）、右侧模型配置状态
- 适合：每天高频使用的长期工作台；信息密度最高

### 变体 B — 卡片流程式（顶部 tab + 单栏卡片流）

```
┌───────────────────────────────────────────────┐
│ logo  职位  简历  学习  面试  设置               │
├───────────────────────────────────────────────┤
│ 标题 + 手动录入                                 │
│ ┌───────────────────────────────┐             │
│ │ 职位卡片：公司·岗位 · 倒计时·状态 │             │
│ │ 标签(企业性质/批次/城市) · 操作  │             │
│ └───────────────────────────────┘             │
│ ┌───────────────────────────────┐             │
│ │ …下一张卡片                     │             │
│ └───────────────────────────────┘             │
│ 步骤条：①简历 ②优化 ③学习 ④面试 ⑤复盘          │
└───────────────────────────────────────────────┘
         （学习聊天为右下浮动层）
```

- 导航：顶部 tab；内容单栏居中（max 960px）卡片流
- 关键差异：学习聊天是**右下浮动窗**；底部常驻**五步闭环步骤条**
- 适合：轻量、逐项处理的流程感

### 变体 C — 职位驾驶舱（职位为中心：窄栏 + 宽活动区）

```
┌──────────────┬────────────────────────────────────┐
│ 当前职位卡     │ 活动区（当前步骤内容）              │
│ (岗位/倒计时)  │ · 匹配度仪表（环形 82 + 5 维度条）  │
│              │ · 优化对比 / 学习聊天 / 面试会话     │
│ 操作闭环：     │ · 复盘卡片                        │
│ 1 优化简历 82  │                                    │
│ 2 学习清单 5 项│                                    │
│ 3 模拟面试 7.5 │                                    │
│ 4 投递跟踪 面试中│                                    │
│──────────────│                                    │
│ 匹配度趋势     │                                    │
│ 基准 64→82    │                                    │
└──────────────┴────────────────────────────────────┘
```

- 导航：左栏 = 当前职位卡 + **四步操作闭环**（编号 1-4，带每步状态/结果）
- 右区 = 活动区：当前步骤的完整视图
- 关键差异：**一切围绕"当前职位"**，操作按匹配度提升闭环排序
- 适合：产品哲学（招聘看匹配度）最直白的表达

## 5. 关键视图（线框级描述，逐视图细化）

> 以下为信息结构与元素清单。三变体共用这些视图，布局按所选变体组织。

### 5.1 职位列表
- 筛选区：企业性质（央企/国企/大厂/私企/外企/事业单位/其他）、批次（提前批/正式批/补录）、投递状态、秋招季
- 职位行/卡片：公司 · 岗位｜倒计时徽标（红=临近）｜企业性质 pill｜批次 · 城市｜投递状态 pill
- 操作：＋手动录入（表单：公司/岗位/性质/JD/城市/渠道+链接/网申起止/批次/备注）；采集入口

### 5.2 职位详情
- 头部：公司 · 岗位 + 性质/批次/城市 + 网申窗口 + 投递渠道链接
- **匹配度概览卡**：总分 + 5 维度条（关键词/技能/项目/经历/学历）+ 「查看依据」入口
- 操作组：✏ 生成优化简历 ｜ 📚 生成学习清单 ｜ 🎤 模拟面试 ｜ ✓ 标记已投
- 主体：JD 全文（等宽可读）、投递记录（时间/渠道/状态）

### 5.3 采集预览
- 摘要条：「将新增 N 条 / 更新 M 条 / 缺字段 K 处」
- 候选列表（可勾选）+ 缺字段标记（如「截止时间待核实」）
- 确认按钮 → 批量入库；留痕记录（最近采集：来源/数量/时间）

### 5.4 简历列表
- 分组：基准简历（技术向 / 国企向…）、优化简历（按职位卡）
- 每项：标题 + 更新时间 + （派生稿：关联职位 + 匹配度 + 来源基准）
- 操作：新建基准、上传 docx/PDF、编辑、A4 预览、导出 PDF

### 5.5 简历编辑器 / 上传解析
- 表单分节：基本信息（含国企字段：政治面貌/生源地/生日/性别）、教育、技能、项目、实习、证书、自我评价
- 上传解析：解析结果 = **草稿态**（带置信度/待确认标记），确认后才入库
- JSON 模式切换（高级用户）

### 5.6 优化对比（核心视图）
- 顶部：匹配度仪表（环形总分 + 5 维度条 + 依据入口）+ 模式徽标（strict）
- 中部：**并排对比**——左基准 / 右优化稿，改动行高亮（增/删/改），每条附理由气泡
- 底部：LLM 定性建议卡（短板 + 建议，可「加入学习清单」）
- 操作：接受并保存（→派生稿入库）/ 编辑后再存

### 5.7 学习清单
- 分组：按优先级（1 JD 硬性要求 → 5 手动）或状态（待学/学习中/已掌握）
- 条目行：名称 + 来源 pill（JD/简历缺口/项目/面试）+ 优先级 + 状态切换
- 空状态/降级：无缺口来源时提示「先做一次简历优化可生成更全的清单」

### 5.8 学习聊天（teach 会话）
- 聊天流：AI 气泡（流式打字机）/ 用户气泡；首条消息即教学开场（跨次续接提示：「上次学到…，继续？」）
- 输入：文本框 + 发送；侧栏：当前清单（点条目切换主题）
- 教学过程中的链接/代码块渲染为卡片

### 5.9 面试准备
- 选择职位卡（默认当前职位）+ 风格选择（真实/教学/压力，带说明文案）
- 启动按钮 + 提示（语音：按住说话；未配置 ASR 时显示文字输入降级说明）

### 5.10 面试会话（核心视图）
- 对话流（打字机渲染面试官文本）
- 底部：**PTT 按住说话按钮**（按住红、松开灰）｜ 文字输入兜底 ｜ 打断/结束按钮
- 阶段提示（开场/技术面/反问/收尾）轻量显示

### 5.11 复盘视图
- 顶部：总分环 + 4 维度（技术深度/表达逻辑/应变/匹配度）+ 一句话总评
- 薄弱点列表：主题 + 细节 + 「加入学习清单」按钮（一键，标来源=面试）
- 参考答案折叠卡（问题 + 参考回答）
- 下一步建议列表；历史回看入口（对话记录回放）

### 5.12 设置
- 模型提供商（下拉：Anthropic/OpenAI/Google…）+ 模型 + API key（密码框）
- 状态行：已配置 ✓ / 未配置（各 agent 功能降级说明）
- 首次启动：引导卡片 → 设置页

## 6. 组件清单

| 组件 | 说明 |
|---|---|
| 导航项 / tab | 变体 A 侧边栏 / B 顶栏 / C 左栏操作闭环 |
| 职位卡片 / 行 | 公司·岗位、倒计时徽标、性质 pill、状态 pill |
| Pill 标签 | 企业性质（大厂蓝/国企橙/央企浅蓝/私企紫）、批次、投递状态（面试中绿/已投灰/收藏空） |
| 倒计时徽标 | 剩 N 天（≤14 天红）；无截止→「待核实」灰 |
| 匹配度仪表 | 环形总分（≥80 绿/60-80 蓝/<60 橙）+ 5 维度条形 |
| 聊天气泡 | AI 灰 / 用户蓝；流式打字机；代码块卡片 |
| PTT 按钮 | 按住=红「松开结束说话」，松开=蓝「按住说话」 |
| 步骤条 | ①简历 ②优化 ③学习 ④面试 ⑤复盘（变体 B 常驻） |
| 降级提示卡 | 未配置模型/ASR 时各视图顶部黄条 + 跳设置 |
| 空状态 | 无职位/无简历/无清单时的引导文案 + 主操作按钮 |
| 确认流程条 | 采集预览「新增/更新/缺字段」摘要；优化稿「确认保存」 |

## 7. 交互要点

- **流式输出**：agent 文本逐字渲染；进行中可打断（面试）/排队（优化）
- **PTT 语音**：按住说话 → 松开识别发送（VAD 静音切句）；面试官回答时可打断
- **确认后入库**：采集预览、上传解析草稿、优化稿——均需用户确认才写库
- **降级路径**：LLM 未配置 → 优化/学习/面试视图顶部黄条引导设置，规则打分仍可用；ASR 缺失 → 面试退化为文字输入
- **键盘**：列表上下键导航、Enter 确认（无障碍基础）

## 8. 设计约束

- 目标窗口：1280×800 起（三栏/驾驶舱布局可到 1440+）
- 简历渲染：A4 794×1123px，`@media print` 输出 PDF（独立于应用 UI 风格）
- 中文排版：微软雅黑/系统字体栈；行高 1.65 用于正文
- 无暗色模式要求（MVP 浅色为主，可后续扩展）

## 9. 待 OpenDesign 细化项

1. **选定变体**（A/B/C 或混搭，如「B 的列表 + C 的匹配度区」）
2. 色彩系统（主色 #2b5ca8 为起始点）、字体、间距/圆角/阴影规范
3. 图标集（导航/操作按钮）
4. 逐视图高保真稿（以上 12 个视图）
5. 空状态与降级提示的具体文案与视觉
6. 流式打字机与 PTT 按钮的微交互
