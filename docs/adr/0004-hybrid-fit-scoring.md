# 混合匹配度模型：规则打分 + LLM 定性

「岗位匹配度」= 规则层 5 维度加权分（关键词覆盖 25% / 技能 25% / 项目相关 20% / 经历相关 15% / 学历专业 15%，0-100，每维度带命中依据清单）+ LLM 层定性分析（短板清单、语义相关识别、改动建议）。

**考虑过的方案**：纯 LLM 定性——不可重复、无法量化；纯规则——词面匹配不懂语义。优化边界用 `optimizationMode` 配置（strict 默认：不虚构仅措辞/重组/聚焦；balanced：允许适度润色），MVP 默认 strict。关联 ticket #9。
